apiVersion: apps/v1
kind: Deployment
metadata:
  name: xslt-transformation-service
  labels:
    app: xslt-transformation-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: xslt-transformation-service
  template:
    metadata:
      labels:
        app: xslt-transformation-service
    spec:
      containers:
        - name: xslt-transformation-service
          image: eaglessoftbv/xslt-transformation-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
          env:
            - name: CONTEXT_PATH
              value: "/"
            - name: LOG_FORMAT
              value: "json"
            - name: LOG_LEVEL
              value: "WARN"
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: xslt-transformation-service
  labels:
    app: xslt-transformation-service
spec:
  type: ClusterIP
  selector:
    app: xslt-transformation-service
  ports:
    - name: http
      port: 80
      targetPort: http

