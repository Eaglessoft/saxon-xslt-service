version: "3.9"

services:
  saxon-xslt-service:
    image: eaglessoftbv/saxon-xslt-service:latest
    container_name: saxon-xslt-service
    ports:
      - "80:8080"
    environment:
      CONTEXT_PATH: "/"
      LOG_FORMAT: "json"
      LOG_LEVEL: "WARN"
    restart: unless-stopped

